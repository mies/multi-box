build:
  box: google/golang
  steps:
    - script:
        name: testing123
        code: go version

deploy:
  box: mongo
  steps:
    - script:
        name: testing deploy section
        code: echo "hello from mongo"
