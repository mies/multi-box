build:
  box: google/golang
  steps:
    - script:
        name: testing build section
        code: go version

deploy:
  box: google/python
  steps:
    - script:
        name: testing deploy section
        code: python --version
