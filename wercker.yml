deploy:
  box: nodesource/node:trusty
  steps:
    - script:
        name: testing build
        code: node --version

build:
  box: python:2.7.9
  steps:
    - script:
        name: testing deploy
        code: python --version
