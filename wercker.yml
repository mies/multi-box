deploy:
  box: google/golang
  steps:
    - script:
        name: testing123
        code: go version

build:
  box: python
  steps:
    - script:
        name: testing deploy section
        code: python --version
