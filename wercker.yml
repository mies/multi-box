build:
  box: nodesource/node:trusty
  steps:
    - script:
        name: testing build
        code: node --version

deploy:
  box: python
  steps:
    - script:
        name: testing deploy
        code: python --version
