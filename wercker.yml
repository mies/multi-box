build:
  box: nodesource/node:trusty
  steps:
    - script:
        name: testing build
        code: node --version

deploy:
  box: google/golang
  steps:
    - script:
        name: testing deploy
        code: go --version
